void obiekt(double* syg, double* Y ){

    static double Y1,Y2,Y3,Y4 = 0;

    for(int i=0; i<dlugSym -1; i++){

    Y4 = (Kp*syg[i] - Y3*(a2 + a1*Tp) - Y2*(a1 +Tp) - Y1)/(a2*Tp*Ti);
    Y3 += Y4*h;
    Y2 += Y3*h +(h*h/2)*Y2;
    Y1 += Y2*h +(h*h/2)*Y3 + (h*h*h/6)*Y4;
    Y[i+1] = Y[i] +h*Y1 + (h*h/2)*Y2 +(h*h*h/6)*Y3+(h*h*h*h/24)*Y4;

    }

}